import { ExtensionProperty, PropertyType } from '@gltf-transform/core';
import { Mapping } from './mapping';
/**
 * # MappingList
 *
 * List of material variant {@link Mapping}s. See {@link MaterialsVariants}.
 */
export declare class MappingList extends ExtensionProperty {
    readonly propertyType = "MappingList";
    readonly parentTypes: PropertyType[];
    readonly extensionName = "KHR_materials_variants";
    static EXTENSION_NAME: string;
    private mappings;
    copy(other: this, resolve?: <T extends import("@gltf-transform/core").Property>(t: T) => T): this;
    /** Adds a {@link Mapping} to this mapping. */
    addMapping(mapping: Mapping): this;
    /** Removes a {@link Mapping} from the list for this {@link Primitive}. */
    removeMapping(mapping: Mapping): this;
    /** Lists {@link Mapping}s in this {@link Primitive}. */
    listMappings(): Mapping[];
}
